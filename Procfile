# This file specifies commands that are executed by the Heroku app on startup.
# It must be placed in the app's root directory.
# The format is "<process type>: <command>"

# Configure the document root
web: vendor/bin/heroku-php-apache2 public/
